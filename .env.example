# Cloudflare Agent Router - Environment Variables
# Copy this file to .env and fill in your values
# 
# Usage:
# 1. cp .env.example .env
# 2. Fill in your Cloudflare credentials below
# 3. Source the file: source .env
# 4. Deploy: just deploy

# =====================
# Cloudflare Deployment Credentials
# =====================
# Required for deploying Workers to Cloudflare
# Get these from: https://dash.cloudflare.com/profile/api-tokens

# API Token with "Edit Cloudflare Workers" permissions
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here

# Your Cloudflare Account ID (found in dashboard sidebar)
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id-here

# =====================
# Instructions
# =====================
# 1. Get API Token:
#    - Visit: https://dash.cloudflare.com/profile/api-tokens
#    - Click "Create Token"
#    - Use "Edit Cloudflare Workers" template
#    - Copy the generated token
#
# 2. Get Account ID:
#    - Visit: https://dash.cloudflare.com/
#    - Account ID is shown in the right sidebar
#
# 3. Set up environment:
#    cp .env.example .env
#    # Edit .env with your values
#    source .env
#    just deploy
#
# 4. For persistent setup, add to your shell profile:
#    echo 'export CLOUDFLARE_API_TOKEN="your-token"' >> ~/.zshrc
#    echo 'export CLOUDFLARE_ACCOUNT_ID="your-id"' >> ~/.zshrc

# =====================
# Development Note
# =====================
# Individual app configs are in:
# - apps/litellm-router/.env (for local dev server secrets)
# - apps/web/.env (if needed)
# 
# This root .env is specifically for deployment credentials.